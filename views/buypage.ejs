<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Checkout</title>
    <style>
        :root {
            --bg1: #fff0f6;
            /* pale pink */
            --bg2: #ffd6ea;
            /* soft pink */
            --card: #fff6fb;
            --accent: #ff4fa8;
            /* bright pink */
            --muted: #6b3653;
        }

        * {
            box-sizing: border-box
        }

        html,
        body {
            height: 100%
        }

        body {
            margin: 0;
            font-family: Inter, 'Segoe UI', Roboto, Arial, sans-serif;
            background: linear-gradient(180deg, var(--bg1), var(--bg2));
            color: var(--muted);
            -webkit-font-smoothing: antialiased
        }

        /* full page centered card */
        .page {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 32px
        }

        .card {
            width: 100%;
            max-width: 920px;
            background: linear-gradient(180deg, var(--card), #fff);
            border-radius: 18px;
            padding: 28px;
            box-shadow: 0 18px 50px rgba(255, 77, 145, 0.12);
            position: relative;
            overflow: hidden
        }

        /* decorative circles */
        .card::before,
        .card::after {
            content: "";
            position: absolute;
            border-radius: 50%;
            opacity: 0.12
        }

        .card::before {
            width: 360px;
            height: 360px;
            background: var(--accent);
            right: -120px;
            top: -120px
        }

        .card::after {
            width: 220px;
            height: 220px;
            background: #ffd0ea;
            left: -80px;
            bottom: -80px
        }

        header {
            display: flex;
            align-items: center;
            gap: 18px;
            margin-bottom: 18px
        }

        .logo {
            width: 68px;
            height: 68px;
            border-radius: 12px;
            background: linear-gradient(135deg, #fff, #ffeaf4);
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 6px 20px rgba(255, 77, 145, 0.08);
            font-weight: 700;
            color: var(--accent)
        }

        h1 {
            font-family: 'Great Vibes', cursive;
            font-size: 2.6rem;
            margin: 0;
            color: var(--accent);
            line-height: 1
        }

        p.lead {
            margin: 6px 0 0;
            color: #7a4867
        }

        .grid {
            display: grid;
            grid-template-columns: 1fr 420px;
            gap: 16px;
            align-items: start
        }

        /* left: form (full height) */
        form {
            background: transparent;
            padding: 6px 0;
            display: flex;
            flex-direction: column;
            gap: 12px
        }

        .quantity-section {
            background: linear-gradient(180deg, #fff, #fff8fb);
            padding: 24px;
            border-radius: 16px;
            border: 1px solid rgba(255, 105, 180, 0.1);
            text-align: center;
            margin-bottom: 20px;
        }

        .quantity-title {
            font-size: 1.2rem;
            font-weight: 700;
            color: var(--muted);
            margin-bottom: 20px;
        }

        .quantity-controls {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 20px;
            margin-bottom: 15px;
        }

        .quantity-btn {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            border: 2px solid var(--accent);
            background: white;
            color: var(--accent);
            font-size: 1.5rem;
            font-weight: bold;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
        }

        .quantity-btn:hover {
            background: var(--accent);
            color: white;
            transform: scale(1.1);
        }

        .quantity-btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
            transform: none;
        }

        .quantity-btn:disabled:hover {
            background: white;
            color: var(--accent);
        }

        .quantity-display {
            font-size: 2.5rem;
            font-weight: 800;
            color: var(--accent);
            min-width: 80px;
            text-align: center;
        }

        .price-section {
            margin-top: 20px;
            padding: 15px;
            background: rgba(255, 255, 255, 0.8);
            border-radius: 12px;
            border: 1px solid rgba(255, 105, 180, 0.1);
        }

        .total-price {
            font-size: 1.4rem;
            font-weight: 800;
            color: var(--accent);
            margin-bottom: 5px;
        }

        .shipping-note {
            font-size: 0.9rem;
            color: var(--muted);
            font-weight: 500;
        }

        .visual-graphic {
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 20px 0;
        }

        .product-image-container {
            width: 160px;
            height: 160px;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 12px 30px rgba(255, 79, 168, 0.3);
            animation: float 3s ease-in-out infinite;
            position: relative;
        }

        .product-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.3s ease;
        }

        .product-image-container:hover .product-image {
            transform: scale(1.05);
        }

        .quantity-visual {
            display: flex;
            align-items: center;
            gap: 5px;
            margin-top: 10px;
            justify-content: center;
        }

        .quantity-dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: var(--accent);
            animation: pulse 2s ease-in-out infinite;
        }

        .quantity-dot:nth-child(2) {
            animation-delay: 0.3s;
        }

        .quantity-dot:nth-child(3) {
            animation-delay: 0.6s;
        }

        .field {
            display: flex;
            flex-direction: column
        }

        label {
            font-weight: 700;
            color: #6b3653;
            font-size: 0.95rem;
            margin-bottom: 6px
        }

        textarea {
            padding: 14px;
            border-radius: 12px;
            border: 1px solid #ffd6ea;
            background: #fff;
            font-size: 1rem
        }

        textarea:focus {
            outline: none;
            box-shadow: 0 6px 20px rgba(255, 77, 145, 0.08);
            border-color: var(--accent)
        }

        textarea {
            min-height: 130px;
            resize: vertical
        }

        .meta {
            background: linear-gradient(180deg, #fff, #fff8fb);
            padding: 18px;
            border-radius: 12px;
            border: 1px solid rgba(255, 105, 180, 0.06)
        }

        .meta h3 {
            margin: 0 0 8px
        }

        .price {
            color: var(--accent);
            font-weight: 800;
            font-size: 1.15rem
        }

        .actions {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 10px
        }

        .note {
            font-size: 0.9rem;
            color: #8a5a73
        }

        .btn {
            background: var(--accent);
            color: #fff;
            padding: 12px 20px;
            border-radius: 12px;
            border: none;
            font-weight: 800;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(255, 79, 168, 0.4);
        }

        @keyframes float {

            0%,
            100% {
                transform: translateY(0px);
            }

            50% {
                transform: translateY(-10px);
            }
        }

        @keyframes pulse {

            0%,
            100% {
                opacity: 1;
                transform: scale(1);
            }

            50% {
                opacity: 0.5;
                transform: scale(0.8);
            }
        }

        .visual-effects {
            margin-top: 20px;
            padding: 20px;
            background: rgba(255, 255, 255, 0.7);
            border-radius: 16px;
            text-align: center;
        }

        .sparkle-container {
            display: flex;
            justify-content: center;
            gap: 10px;
            margin-bottom: 15px;
        }

        .sparkle {
            width: 8px;
            height: 8px;
            background: var(--accent);
            border-radius: 50%;
            animation: sparkle 1.5s ease-in-out infinite;
        }

        .sparkle:nth-child(1) {
            animation-delay: 0s;
        }

        .sparkle:nth-child(2) {
            animation-delay: 0.3s;
        }

        .sparkle:nth-child(3) {
            animation-delay: 0.6s;
        }

        .sparkle:nth-child(4) {
            animation-delay: 0.9s;
        }

        .sparkle:nth-child(5) {
            animation-delay: 1.2s;
        }

        .floating-hearts {
            position: relative;
            height: 40px;
            margin: 10px 0;
        }

        .floating-heart {
            position: absolute;
            font-size: 1.2rem;
            animation: floatUp 4s ease-in-out infinite;
            opacity: 0;
        }

        .floating-heart:nth-child(1) {
            left: 20%;
            animation-delay: 0s;
        }

        .floating-heart:nth-child(2) {
            left: 40%;
            animation-delay: 1s;
        }

        .floating-heart:nth-child(3) {
            left: 60%;
            animation-delay: 2s;
        }

        .floating-heart:nth-child(4) {
            left: 80%;
            animation-delay: 3s;
        }

        @keyframes sparkle {

            0%,
            100% {
                opacity: 0.3;
                transform: scale(0.8);
            }

            50% {
                opacity: 1;
                transform: scale(1.2);
            }
        }

        @keyframes floatUp {
            0% {
                transform: translateY(20px) scale(0.8);
                opacity: 0;
            }

            50% {
                opacity: 1;
            }

            100% {
                transform: translateY(-20px) scale(1.2);
                opacity: 0;
            }
        }

        .glow-text {
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--accent);
            text-shadow: 0 0 10px rgba(255, 79, 168, 0.3);
            animation: glow 2s ease-in-out infinite alternate;
        }

        @keyframes glow {
            from {
                text-shadow: 0 0 10px rgba(255, 79, 168, 0.3);
            }

            to {
                text-shadow: 0 0 20px rgba(255, 79, 168, 0.6);
            }
        }

        @media(max-width:920px) {
            .grid {
                grid-template-columns: 1fr
            }

            .card::before {
                display: none
            }

            .card::after {
                display: none
            }
        }

        @media(max-width:420px) {
            h1 {
                font-size: 2rem
            }

            .quantity-controls {
                gap: 15px;
            }

            .quantity-btn {
                width: 45px;
                height: 45px;
                font-size: 1.3rem;
            }

            .quantity-display {
                font-size: 2rem;
            }

            .product-image-container {
                width: 140px;
                height: 140px;
            }
        }
    </style>
</head>

<body>
    <div class="page">
        <div class="card">
            <header>
                <div>
                    <h1>Place your order</h1>
                    <p class="lead">Select quantity and add any special requests for your order.</p>
                </div>
            </header>

            <div class="grid">
                <div>
                    <form action="/Confirm-purchase" method="POST" novalidate>
                        <input type="hidden" name="productName" value="<%= productName %>">
                        <input type="hidden" name="productCollection" value="<%= productcollection %>">
                        <input type="hidden" name="quantity" id="quantityInput" value="1">
                        <input type="hidden" name="buyerName" value="<%= userDetails.name %>">
                        <input type="hidden" name="contactNumber" value="<%= userDetails.contactNumber %>">
                        <input type="hidden" name="address" value="<%= userDetails.address %>">


                        <!-- Quantity Section -->
                        <div class="quantity-section">
                            <div class="quantity-title">Select Quantity</div>

                            <!-- Visual Graphic with Product Image -->
                            <div class="visual-graphic">
                                <div class="product-image-container">
                                    <img src="<%= product.image %>" alt="<%= productName %>" class="product-image">
                                </div>
                            </div>

                            <!-- Quantity Controls -->
                            <div class="quantity-controls">
                                <button type="button" class="quantity-btn" id="decreaseBtn">-</button>
                                <div class="quantity-display" id="quantityDisplay">1</div>
                                <button type="button" class="quantity-btn" id="increaseBtn">+</button>
                            </div>

                            <div class="quantity-visual">
                                <div class="quantity-dot"></div>
                                <div class="quantity-dot"></div>
                                <div class="quantity-dot"></div>
                            </div>

                            <!-- Price Section -->
                            <div class="price-section">
                                <div class="total-price" id="totalPrice">₹<%= product.price %>
                                </div>
                                <div class="shipping-note">+ shipping charges</div>
                            </div>
                        </div>

                        <!-- Customization Field -->
                        <div class="field">
                            <label for="customize">Customize (optional)</label>
                            <textarea id="customize" name="customize"
                                placeholder="Add any customization requests, notes for the seller... (e.g. gift wrap, color preference, special instructions)"></textarea>
                        </div>


                        <div class="actions">
                            <span class="note">We'll contact you to confirm details</span>
                            <button class="btn" id="placeOrderBtn" type="submit">Place order</button>
                        </div>
                    </form>
                </div>

                <aside class="meta">
                    <h3>Need help?</h3>
                    <p>If you have questions about sizes, colors or shipping, reply to the confirmation email or call us
                        at <strong>+91 80000 00000</strong>.</p>

                    <h3 style="margin-top:16px">Order info</h3>
                    <p class="price">We'll confirm final price & shipping after you submit.</p>

                    <!-- Visual Elements Only -->
                    <div
                        style="margin-top: 20px; padding: 20px; background: rgba(255, 255, 255, 0.5); border-radius: 12px; text-align: center;">
                        <div style="display: flex; justify-content: center; gap: 15px; margin-bottom: 15px;">
                            <div
                                style="width: 10px; height: 10px; background: var(--accent); border-radius: 50%; animation: pulse 2s infinite;">
                            </div>
                            <div
                                style="width: 10px; height: 10px; background: var(--accent); border-radius: 50%; animation: pulse 2s infinite 0.5s;">
                            </div>
                            <div
                                style="width: 10px; height: 10px; background: var(--accent); border-radius: 50%; animation: pulse 2s infinite 1s;">
                            </div>
                        </div>
                        <div style="font-size: 2rem; animation: float 3s ease-in-out infinite;">🎯</div>
                    </div>
                </aside>
            </div>
        </div>
    </div>
    <script>
        // Quantity control functionality
        document.addEventListener('DOMContentLoaded', function () {
            const decreaseBtn = document.getElementById('decreaseBtn');
            const increaseBtn = document.getElementById('increaseBtn');
            const quantityDisplay = document.getElementById('quantityDisplay');
            const quantityInput = document.getElementById('quantityInput');
            const totalPrice = document.getElementById('totalPrice');
            const placeOrderBtn = document.getElementById('placeOrderBtn');
            const form = document.querySelector('form');

            let quantity = 1;
            const unitPrice = <%= product.price %>;

            function updateQuantity() {
                quantityDisplay.textContent = quantity;
                quantityInput.value = quantity;

                // Calculate and update total price
                const total = quantity * unitPrice;
                totalPrice.textContent = `₹${total.toLocaleString()}`;

                // Update button states
                decreaseBtn.disabled = quantity <= 1;
            }

            decreaseBtn.addEventListener('click', function () {
                if (quantity > 1) {
                    quantity--;
                    updateQuantity();
                }
            });

            increaseBtn.addEventListener('click', function () {
                quantity++;
                updateQuantity();
            });

            // Replace button text with "Processing..." on submit (without loader)
            if (form && placeOrderBtn) {
                form.addEventListener('submit', function (e) {
                    placeOrderBtn.disabled = true;
                    placeOrderBtn.textContent = 'Processing...';
                });
            }

            // Initialize
            updateQuantity();
        });
    </script>
</body>

</html>