<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Order Confirmed</title>
    <style>
        /* Pink themed confirmation page */
        *,
        *:before,
        *:after {
            box-sizing: border-box;
        }

        html,
        body {
            height: 100%;
            margin: 0;
            font-family: Inter, system-ui, -apple-system, 'Segoe UI', Roboto, Arial;
        }

        body {
            background: linear-gradient(180deg, #fff0f6 0%, #ffd6ea 60%);
            color: #5b3b48;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .card {
            width: 100%;
            max-width: 900px;
            background: rgba(255, 255, 255, 0.9);
            border-radius: 16px;
            box-shadow: 0 20px 40px rgba(183, 52, 108, 0.08);
            overflow: hidden;
            display: flex;
            flex-direction: column;
            min-height: 70vh;
        }

        .top {
            flex: 1 1 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 40px 28px;
            text-align: center;
        }

        .confirmation-title {
            font-size: 1.9rem;
            color: #ff4fa8;
            margin: 0 0 12px 0;
            font-weight: 800;
        }

        .confirmation-sub {
            font-size: 1rem;
            color: #6b4a59;
            line-height: 1.5;
            margin: 0;
        }

        .bottom {
            flex: 1 1 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 18px;
            background: linear-gradient(180deg, transparent, rgba(255, 255, 255, 0.6));
        }

        .order-img {
            max-width: 70%;
            max-height: 45vh;
            width: auto;
            height: auto;
            display: block;
            object-fit: contain;
            filter: drop-shadow(0 12px 24px rgba(183, 52, 108, 0.08));
        }

        .actions {
            margin-top: 16px;
        }

        .btn {
            display: inline-block;
            padding: 10px 16px;
            border-radius: 10px;
            text-decoration: none;
            font-weight: 700;
            background: #ff4fa8;
            color: #fff;
            box-shadow: 0 8px 20px rgba(255, 79, 168, 0.12);
        }

        @media (max-width: 720px) {
            .confirmation-title {
                font-size: 1.4rem;
            }

            .order-img {
                max-width: 90%;
                max-height: 35vh;
            }

            .card {
                min-height: 60vh;
            }
        }
    </style>
</head>

<body>
    <script>
        // Remove the form page from browser history
        history.replaceState(null, "", "/");
    </script>
    <div class="card">
        <div class="top">
            <div>
                <h1 class="confirmation-title">Order Confirmed</h1>
                <p class="confirmation-sub">Thank you! Your order has been received and is being processed. You will
                    receive the order details shortly.</p>
                <div class="actions">
                    <a class="btn" href="/products">Continue shopping</a>
                </div>
            </div>
        </div>
        <div class="bottom">
            <img src="/pictures/order.png" alt="order confirmed" class="order-img"
                onerror="this.src='/pictures/11.png'">
        </div>
    </div>
</body>

</html>